{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Launch Sorting Visualizer",
      "type": "coreclr",
      "request": "launch",
      "preLaunchTask": "build",
      "program": "${workspaceFolder}/bin/Debug/net8.0-windows/SortingVisualizer.exe",
      "cwd": "${workspaceFolder}"
    }
  ]
}
